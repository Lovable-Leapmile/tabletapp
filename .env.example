# API Base URL - Used for all API calls throughout the project
# Change this to point to your backend server
VITE_BASE_URL=https://robotmanagerv1test.qikpod.com

# App Base Path - Used for deploying under a subpath (e.g., /standard-app/)
# Only needed if deploying to a subdirectory, defaults to "/"
# VITE_APP_BASE=/
